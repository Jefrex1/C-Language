#include <stdio.h>

int main() {
    float a, b, c;
    printf("Vvedit tryvalist rozmovu: ");
    scanf("%f", &a);
    
    if (a < 10) { 
        b = a * 1.2;
        printf("%f ", b);
    }
    if (a > 10) {
        b = a * 1;
        printf("%f ", b);
        
    }
    
    return 0;
}
